fragment PlayerData on Player {
  id

  isReady

  role

  name
  facts

  experience
  experienceToNext

  achievementKeys
  achievements {
    id
    count

    achievement {
      id
      name
      description
      image
      reward
    }
  }

  level {
    id
    index
  }
}
