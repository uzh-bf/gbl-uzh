{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "format": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "dev": {
      "cache": false,
      "outputs": ["dist/**", ".next/**"],
      "dependsOn": ["@gbl-uzh/platform#build"]
    },
    "start": {
      "dependsOn": ["^build", "build"],
      "outputs": ["dist/**", ".next/**"]
    }
  }
}
