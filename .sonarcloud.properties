# Read by SonarCloud automatic analysis (no CI-based scanner in this repo),
# and only from the default branch — a change here affects analyses after merge.
# The demo game is the scaffold every example game copies (docs/adr/0002), so
# the per-app NextAuth glue is intentionally identical across apps; it cannot
# move into @gbl-uzh/platform because it imports app-local prisma/jwt modules.
# Literal paths: SonarCloud documents wildcards as unsupported in this file.
sonar.cpd.exclusions=apps/demo-game/src/lib/authOptions.ts,examples/central-bank/src/lib/authOptions.ts,examples/rate-wars/src/lib/authOptions.ts
